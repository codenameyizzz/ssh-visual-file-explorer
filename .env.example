# Optional local server settings.
# The app will still run without this file and default to PORT=3000 and HOST=0.0.0.0.
PORT=3000
HOST=0.0.0.0

# Optional Vite flag. Set to true only if you want to disable HMR.
DISABLE_HMR=false
